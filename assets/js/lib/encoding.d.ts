/** Encodes a UTF-8 string as base64. */
export declare function toBase64(value: string): string;
/** Produces the base64 identifier format used by existing DOM elements. */
export declare function idify(value: string): string;
